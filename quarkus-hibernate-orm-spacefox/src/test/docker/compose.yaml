services:

  db-spacefox:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_DB: quarkus-spacefox
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
    ports:
      - "5432:5432"
