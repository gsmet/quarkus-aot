services:

  db-simple:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_DB: quarkus-simple
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
    ports:
      - "5434:5432"
